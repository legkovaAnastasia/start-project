import styled from "styled-components";

const Header = styled.header`
    background-color: rgba(38, 50, 56, 0.7);
    padding: 20px 0;
    position: sticky;
    top: 0;
    left: 0;
    right: 0;
    z-index: 999;
`
export const S = {
    Header
}